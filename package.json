{
  "name": "neurosyn-copilot",
  "version": "3.0.0",
  "description": "Engineering Intelligence OS",
  "type": "module",
  "scripts": {
    "build": "npm --prefix backend install && npm --prefix frontend install && npm --prefix frontend run build",
    "start": "cd backend && node app.js"
  }
}